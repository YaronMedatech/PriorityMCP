# ---------------------------------------------------------------------------
# Priority OData connection
# ---------------------------------------------------------------------------
# Two forms, and which one you use decides whether callers can choose a company.
#
#   ONE COMPANY -- end the URL with the company name:
#     https://<host>/odata/Priority/<tabula.ini>,<lang>/<company>
#
#   SEVERAL COMPANIES -- end the URL at the tabula.ini and list the companies in
#   PRIORITY_ENVIRONMENTS below:
#     https://<host>/odata/Priority/<tabula.ini>,<lang>
#
# The server tells the two apart by whether the last path segment looks like a
# tabula.ini, so both work with no extra flag.
PRIORITY_ODATA_URL=https://your-host.example.com/odata/Priority/tabula.ini,1

# Companies a caller may select, separated by semicolons. The first is the
# default for a session that does not ask for one.
#
# It is an ALLOWLIST, not a hint: the name is concatenated into the OData URL
# path, so anything not listed here is refused. That check is what stops
# `../../tabula.ini,1/othercompany` from escaping through a request header.
#
# Leave BLANK if PRIORITY_ODATA_URL already ends with a company.
#
# Switching company changes the DATA and nothing else. Screen definitions,
# column titles and help live at the tabula.ini level, so every company on one
# installation has the same dictionary -- which is why the dictionary cache is
# shared across them and a switch does not trigger a re-fetch.
PRIORITY_ENVIRONMENTS=

# Where this Priority runs: 'cloud' (Priority's own cloud, *.priority-connect.online)
# or 'self-hosted' (your own or a partner's servers). Leave EMPTY to detect from
# the OData host name; the startup log says what was detected and how to pin it.
#
# Set it explicitly when this .env travels between installations -- a heuristic
# that is right for one host name is a silent wrong guess on the next. Today it
# changes two things, both in the Web SDK channel (programs and reports):
#   - the service URL:  cloud = https://<host>/wcf/service.svc,
#                       self-hosted = the host root (SDK appends /wcf/wcf/Service.svc)
#   - which identity is tried first:  cloud = the PAT, self-hosted = PRIORITY_USER
# OData behaves the same on both.
PRIORITY_HOSTING=

# Authentication -- a PAT token, a username + password, or both. Keep both when
# one .env serves several installations: OData uses the PAT when present; the
# Web SDK tries them in the order PRIORITY_HOSTING calls for and falls back to
# the other. A PAT is sent as basic auth with the literal string "PAT" as the
# password.
PRIORITY_API_TOKEN=
PRIORITY_USER=
PRIORITY_PASS=

# Where a SESSION's Priority identity comes from. This is separate from the
# credentials above: those are the server's own identity, this decides whether
# callers act as themselves instead.
#
#   shared   (default) everyone acts as the identity above. Right for a single
#            operator. A token-holder sees whatever that identity sees.
#   headers  each caller supplies its own in its client config -- X-Priority-User
#            + X-Priority-Pass, or X-Priority-Token. Requires TLS. See the bottom
#            of this file.
#   elicit   the CLIENT asks its user at first use, through its own UI. The answer
#            travels client -> server and never enters the model's context, which
#            is why there is no login() tool: a tool argument is chosen by the
#            model and would land in the transcript and the logs.
#
# NOT EVERY CLIENT SUPPORTS elicit. Claude Code and VS Code Copilot do; Claude
# Desktop and Gemini CLI do not -- Gemini CLI answers `Method not found`. The
# fallback decides what happens then: `shared` uses the identity above, `refuse`
# fails the session rather than quietly acting as someone else.
PRIORITY_AUTH_MODE=shared
PRIORITY_AUTH_FALLBACK=shared

# TLS. Most on-prem Priority servers use a self-signed certificate.
#   PRIORITY_CA_BUNDLE  -- preferred: keep verifying, trust their certificate.
#   PRIORITY_VERIFY_SSL -- set to 0 to stop verifying entirely. Acceptable only
#                          on a trusted internal network; gives up MITM protection.
PRIORITY_VERIFY_SSL=1
PRIORITY_CA_BUNDLE=

# Overall budget for one Priority query, in milliseconds, spanning every retry.
# Keep it below your MCP client's request deadline (commonly 60s): if Priority is
# allowed to run longer than the client waits, the client times out first and you
# get a generic error instead of this server's explanation.
PRIORITY_TIMEOUT_MS=45000

# Ceiling on requests ONE SESSION may send Priority in a rolling minute. Every
# other limit here bounds a single call (aggregate's maxRows, query's 500 rows,
# describe_screen's metadata budget); this one bounds the calls, because the
# expensive tools are expensive by paging -- one aggregate over 50,000 rows is
# hundreds of sequential requests. It exists to stop a model in a retry loop from
# hammering a production ERP, so it is set well above normal use. Set 0 to lift it.
#
# Per session, not global: over HTTP each session gets its own budget, and in
# stdio each client is its own process. It does not bound several sessions
# together. Going over it fails the call outright rather than waiting past the
# deadline -- a limiter that sleeps past it would report as a Priority timeout.
PRIORITY_MAX_REQUESTS_PER_MIN=240

# ---------------------------------------------------------------------------
# Ceilings on one tool call's result
# ---------------------------------------------------------------------------
# All four default to what these tools shipped with, and all four take 0 to mean
# NO CEILING -- the same spelling as the budget above, so there is one convention
# rather than four.
#
# The two halves buy different things, and it is worth knowing which one you are
# raising:
#
#   ROWS_PER_QUERY and RESPONSE_CHARS bound what lands in the MODEL'S CONTEXT.
#   Raising them does not let a model handle more data. Every row query returns is
#   read by the model, so a bigger reply crowds out the conversation it was meant
#   to inform, costs proportionally more, and past a point the client drops the
#   turn outright. There is no value here that makes 50,000 detail rows usable in
#   a chat.
#
#   SCAN_ROWS and GROUPS bound what AGGREGATE reads while paging. Those rows cost
#   requests and time and NO context at all -- they are summed inside this server
#   and only the group rows are returned. This is the half to raise when an answer
#   genuinely needs more data behind it, and it is the reason 'aggregate' exists
#   instead of 'query' with a bigger cap.
#
# Raising SCAN_ROWS interacts with PRIORITY_MAX_REQUESTS_PER_MIN above: a scan
# reads 500 rows per request, so 200,000 rows is 400 requests and will hit a
# 240/minute budget. Raise both or neither.

# Hard maximum for query's 'top'. The default is this server's own page ceiling.
PRIORITY_MAX_ROWS_PER_QUERY=500

# Character ceiling on a query reply, applied independently of the row count --
# 500 rows of a 200-column screen blows this long before it blows the row cap.
# A truncated reply always says so; it is never presented as complete.
PRIORITY_MAX_RESPONSE_CHARS=200000

# Rows aggregate will page through before stopping and marking the totals a lower
# bound. 0 lets a scan run until the data ends.
PRIORITY_MAX_SCAN_ROWS=50000

# Distinct groups aggregate will create before it stops and says so. Guards
# against grouping by an identifier by accident, which turns an answer into a row
# dump. Unlike the scan ceiling, every group DOES land in the reply.
PRIORITY_MAX_GROUPS=5000

# ---------------------------------------------------------------------------
# Web SDK channel -- only needed to RUN programs and reports
# ---------------------------------------------------------------------------
# OData cannot run a Priority program at all, so that goes over the Web SDK,
# which is a separate channel with separate rules:
#   - Identity: the PAT works here too (sent as username=<token>, password='PAT',
#     documented since Priority 19.1) and is used when PRIORITY_API_TOKEN is set,
#     so both channels act as the same user. PRIORITY_USER / PRIORITY_PASS are the
#     fallback. Measured 2026-09-02 on the cloud: the PAT logged in, the named
#     user was refused with the SDK's generic "Can't connect to server".
#   - URL: NOT the OData path. Self-hosted: the host root (https://your-host/) and
#     the SDK appends /wcf/wcf/Service.svc. Priority's cloud
#     (*.priority-connect.online): https://<host>/wcf/service.svc -- one "wcf",
#     ending in .svc so the SDK appends nothing -- per Priority's SDK docs
#     (prioritysoftware.github.io/api/global/#login). The derived
#     /wcf/wcf/Service.svc answers 403 there.
#
# Both of the settings below are derived from PRIORITY_ODATA_URL automatically
# (including the cloud rule above), so leave them blank unless your installation
# genuinely differs.
PRIORITY_HOST_URL=
PRIORITY_COMPANY=
PRIORITY_TABULAINI=

# Catalog of programs, with the operator's own description and notes for each.
# Priority offers no way to enumerate runnable programs (APPS/APP answer 404), so
# by default this file is BOTH the permission and the discovery surface.
PRIORITY_PROGRAMS_FILE=programs.json

# Set to 1 to allow ANY procedure (P) or report (R) in the installation to be
# run, not only the catalogued ones -- 9,229 of them on the reference server.
#
# THINK BEFORE TURNING THIS ON. Until now the catalog was the only thing standing
# between a model and every procedure Priority has, including ones that post,
# delete, or start an upgrade: KAR_EXECUPGRADES runs a live DBI upgrade when
# handed a file path. With this on, the model is trusted to read a program's help
# and ask the user before running a procedure -- which its instructions tell it
# to do, and which is a rule rather than a mechanism.
#
# The catalog still matters when this is on: catalogued programs carry notes, and
# anything else comes back with a 'caution' saying nothing is documented about it.
#
# What this does NOT loosen: a name Priority does not have is still refused (the
# dictionary is checked, so a typo cannot become a run); a name that is both a
# procedure and a report is refused until the type is given; a first call without
# inputs still only reports parameters; a choice is still never made server-side.
PRIORITY_ALLOW_ALL_PROGRAMS=0

# Programs that must never run, whatever the setting above -- separated by ; or ,
# and matched case-insensitively. This is the counterpart to opening the policy:
# name the handful that would be damaging and let the rest through.
#   PRIORITY_PROGRAMS_DENY=KAR_EXECUPGRADES;WWWSHOWPRICE
PRIORITY_PROGRAMS_DENY=

# ---------------------------------------------------------------------------
# Tool exposure
# ---------------------------------------------------------------------------
# get_sales is the curated sales shortcut. It is HIDDEN by default so the generic
# discovery tools get exercised instead -- with it available the model reaches for
# it and never touches search_screens/describe_screen/query. Set to 1 to expose it.
PRIORITY_ENABLE_GET_SALES=0

# AI skills authored inside Priority (the AIWORKFLOWS screen, "AI סקילז"), as
# list_skills / get_skill. OFF by default: deferred by the operator on 2026-09-02,
# and the screen is not opened for the API on the reference installation (it
# answers "לא ניתן להפעיל API למסך זה" -- the API user CAN read other screens of
# the same module, so it is the screen's own API flag, not a permission). Set to 1
# to expose the tools; they report a closed screen as a reason, never as "no
# skills". The .env of a running service is read only at start: restart after.
PRIORITY_ENABLE_SKILLS=0

# Read-only mode. Set to 1 and run_program is not registered at all, which leaves
# this server with no way to change anything in Priority: OData here is only ever
# read, so run_program is the single tool that can act.
#
# It takes away ONLY that. The discovery and read tools are never gated -- without
# search_screens and describe_screen a model cannot learn a screen name and falls
# back to inferring one from its English code, which is the mistake this server
# was built to stop (see "Why discovery rather than a method per domain").
#
# list_programs stays, so the catalogue can still be read, and says that running
# is disabled. The server's own instructions to the model change too, rather than
# only the registration: a model told it has a way to act keeps looking for it.
PRIORITY_READ_ONLY=0

# ---------------------------------------------------------------------------
# Claude (used by the chat clients only -- the MCP server never calls Claude)
# ---------------------------------------------------------------------------
# Optional: the SDK also picks up an `ant auth login` profile automatically.
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-opus-5

# ---------------------------------------------------------------------------
# Network access (npm run server:http) -- only needed to serve OTHER machines
# ---------------------------------------------------------------------------
# The stdio server (npm run server) needs none of this. These apply to the HTTP
# transport, which listens on the LAN so Claude on another machine can use the
# tools.
#
# MCP_AUTH_TOKEN is REQUIRED whenever the listener is not loopback-only, and the
# server refuses to start without it rather than warning. This process holds
# Priority credentials and every tool reads live ERP data, so an unauthenticated
# port is an open database. Minimum 16 characters; generate one with:
#   powershell -c "[guid]::NewGuid().ToString() + [guid]::NewGuid().ToString('N').Substring(0,8)"
MCP_AUTH_TOKEN=

# Defaults: 3401 on 0.0.0.0. Set MCP_HTTP_HOST=127.0.0.1 to bind loopback only
# (the token then becomes optional, e.g. when tunnelling over SSH instead).
MCP_HTTP_PORT=3401
MCP_HTTP_HOST=0.0.0.0

# How long a session may sit idle before the server drops it, in ms (default 30
# minutes). This is not optional housekeeping: an MCP client's close() sends
# NOTHING on the wire -- only an explicit terminateSession() does -- so a client
# that crashes or drops off the network would otherwise leave its session, and the
# server object behind it, alive forever.
MCP_SESSION_IDLE_MS=1800000

# ---------------------------------------------------------------------------
# Natural-language support files
# ---------------------------------------------------------------------------
# Business term -> screen mapping, and worked examples. Both are optional and
# both degrade quietly if absent, but the glossary is the single highest-value
# input to natural-language accuracy: Priority's screen titles are Priority's
# wording, not the words people actually use.
PRIORITY_GLOSSARY_FILE=glossary.json
PRIORITY_EXAMPLES_FILE=examples.json

# Per-caller Priority identities. Without it, every caller shares the single
# identity below and sees everything it can see. See users.example.json.
MCP_USERS_FILE=users.json

# ---------------------------------------------------------------------------
# TLS — encrypts the channel, and is REQUIRED for caller-supplied credentials
# ---------------------------------------------------------------------------
# Generate with:  powershell -ExecutionPolicy Bypass -File scripts/make-cert.ps1
# It prints these two lines filled in. Setting MCP_TLS_PFX switches the listener
# to HTTPS; a certificate configured but unreadable REFUSES to start rather than
# silently serving plaintext.
MCP_TLS_PFX=
MCP_TLS_PFX_PASSWORD=
# PEM alternative, if you already have a certificate from elsewhere.
MCP_TLS_CERT=
MCP_TLS_KEY=

# With TLS on, a caller may present its OWN Priority identity in headers set in
# its MCP config -- X-Priority-User + X-Priority-Pass, or X-Priority-Token for a
# PAT. Headers, never tool arguments: an argument would be chosen by the model and
# would land in the conversation and the logs. Over plain HTTP these headers are
# refused with a 400.
